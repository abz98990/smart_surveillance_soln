# Tested on Python 3.9-3.12. Everything here is actually imported.

ultralytics>=8.3.0,<9      # pulls in torch, torchvision, pillow
opencv-python>=4.8.0
numpy>=1.24,<3
PyYAML>=6.0
Flask>=3.0
plyer>=2.1                 # optional: without it, desktop alerts are skipped

# Tests use the standard library only:
#   python -m unittest discover -s tests -t .
